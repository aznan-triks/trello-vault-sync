{
	"name": "trello-vault-sync",
	"version": "1.17.2",
	"description": "Two-way Trello sync and link auditing for Obsidian worldbuilding vaults.",
	"main": "main.js",
	"type": "module",
	"private": true,
	"scripts": {
		"sync:changelog": "node scripts/sync-changelog.mjs",
		"dev": "node esbuild.config.mjs",
		"build": "npm run sync:changelog && tsc --noEmit && node esbuild.config.mjs production",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"check:hardcode": "node scripts/check-hardcode.mjs",
		"check": "npm run sync:changelog && npm run typecheck && npm run check:hardcode && npm run test && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"trello",
		"sync",
		"worldbuilding"
	],
	"author": "aznan-triks",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.10.2",
		"esbuild": "^0.28.2",
		"obsidian": "^1.7.2",
		"typescript": "^5.7.2",
		"vitest": "^4.1.11"
	}
}
