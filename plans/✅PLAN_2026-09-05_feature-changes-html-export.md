type: feature
init: full
Terminé quand : `npm run check` vert (typecheck + tests, dont `tests/changesHtml.test.ts` pur pour le rendu HTML et un nouveau bloc dans `tests/auditChanges.test.ts`-like pour `features/exportChangesHtml.ts` avec un fetcher d'avatar factice, + build) ; la commande "Export change log as HTML" apparaît dans la palette et le panneau latéral (section Vault) ; une carte sans avatar (auteur sans `avatarUrl`) produit un HTML valide (repli sur une initiale, pas d'image cassée) ; deux entrées du même auteur ne déclenchent qu'un seul téléchargement d'avatar ; passage réel documenté dans `test-vault/` si l'accès écran redevient possible, sinon noté en Écarts (même blocage §10 que tous les sous-plans depuis le 2026-09-04).
État actuel observé : N/A — pas un bug, décision explicite de l'utilisateur (clarification 2026-09-05) : une commande séparée (indépendante du moment où le changelog Markdown a été généré) qui produit une page HTML autonome du changelog, avec l'avatar Trello de chaque auteur téléchargé et intégré (embarqué en base64, pas un lien externe — fonctionne hors-ligne, un seul fichier à partager).
Ressources consultées :
- `src/features/auditChanges.ts` — patron exact à suivre pour la nouvelle feature : `(vault, client, options, reporter, signal) => Promise<Result>`, réutilise `client.getActions()` + `describeAction()`. Différence : n'avance jamais `auditChangesCursor` (ce n'est pas l'audit canonique, juste une vue alternative du même flux — l'audit Markdown reste la seule source qui consomme le curseur).
- `src/core/auditAction.ts` (`TrelloAction.memberCreator`, `AuditEntry.author`) — `memberCreator` n'expose aujourd'hui que `fullName` ; Trello renvoie aussi `avatarUrl` sur ce même sous-objet si on le demande via le paramètre `memberCreator_fields` de l'API (pas un nouvel appel réseau par carte, juste un champ de plus sur l'appel `getActions` déjà fait).
- `src/trello/client.ts::getActions()` — étendu avec `memberCreator_fields: "avatarUrl,fullName"`, même patron que `fields`/`CARD_FIELDS` déjà en place ailleurs dans ce fichier (constante de protocole, pas un réglage).
- `src/core/auditReport.ts::groupChangesByDayThenCard` (sous-plan 1/3, déjà livré) — regroupement jour→carte déjà écrit et testé ; à exporter (au lieu de rester privé) pour être réutilisé tel quel par le nouveau rendu HTML, plutôt que dupliqué (DRY, retenu par la revue Standards du sous-plan 1).
- `src/obsidian/transport.ts` (`obsidianTransport`, `requestUrl`) — seul endroit du projet qui a le droit d'appeler `requestUrl` (§9). Un avatar Trello est une URL publique (pas d'authentification, pas l'API `/1/...`) : pas besoin de faire transiter ça par `TrelloClient`/son retry — une fonction dédiée `obsidianDownloadBinary(url): Promise<ArrayBuffer | null>` dans ce même fichier, `null` sur échec (image manquante ou hors-ligne, jamais fatal pour tout l'export).
- `src/obsidian/gateway.ts` (`VaultGateway.create`/`write`) — suffisant tel quel : l'export HTML est un fichier texte (le base64 est du texte dans l'attribut `src`), aucune écriture binaire de fichier séparé nécessaire dans le coffre.
- `src/features/boardIndex.ts`/`auditLocations.ts` (patron d'injection : `client`/`vault` passés en paramètre, jamais construits dans `features/`) — même patron pour le nouveau fetcher d'avatar : injecté en paramètre de la feature (`fetchBinary: (url: string) => Promise<ArrayBuffer | null>`), pas importé depuis `obsidian/` directement dans `features/` (isolation §1.2 : `features/` ne dépend que d'interfaces/fonctions injectées, jamais de `obsidian` directement — même règle que `VaultGateway`/`TrelloClient`).
- `src/settings/types.ts`/`SettingsTab.ts::renderScope` (`reportPath`, `VaultPathSuggest` sur `folderCandidates()`) — patron à suivre pour le nouveau réglage `changesHtmlPath` : un champ texte + autocomplétion sur les dossiers existants (comme "Synced folder"), pas sur les notes Markdown (la destination est un `.html`, qui n'existe pas forcément encore).
- `src/commands/auditCommands.ts`/`registry.ts` (patron exact des 3 commandes d'audit existantes) — nouvelle commande `audit-changes-html`, section Vault, bouton automatique dans le panneau latéral.
Décision :
- Étend `src/core/auditAction.ts` : `TrelloAction.memberCreator.avatarUrl?: string`, `AuditEntry.authorAvatarUrl?: string` (passthrough dans `describeAction()`).
- Étend `src/trello/client.ts::getActions()` : `memberCreator_fields: "avatarUrl,fullName"` dans les params de la requête.
- Étend `src/core/auditReport.ts` : exporte `groupChangesByDayThenCard` (déjà écrite, juste retirée de `function` privée).
- Crée `src/core/changesHtml.ts` (nouveau, pur) : `ChangesHtmlEntry` (`AuditEntry & { avatarDataUri: string | null }`), `buildChangesHtml(entries, { timestamp }): string` — même structure jour→carte que le rapport Markdown, avatar en `<img src="data:image/png;base64,...">` si disponible, sinon un cercle avec l'initiale du nom de l'auteur (pas d'image cassée).
- Étend `src/obsidian/transport.ts` : `obsidianDownloadBinary(url): Promise<ArrayBuffer | null>` — `requestUrl` avec `throw: false`, retourne `null` sur statut non-2xx ou erreur transport (jamais d'exception qui casserait tout l'export pour un seul avatar manquant).
- Crée `src/features/exportChangesHtml.ts` (nouveau, fonction, patron `auditChanges.ts`) : appelle `client.getActions({ since })`, `describeAction()`, déduplique les téléchargements d'avatar par `avatarUrl` (une entrée en cache par url, jamais deux fois le même auteur), encode en base64, construit les `ChangesHtmlEntry[]`, appelle `buildChangesHtml()`, écrit via `vault.create()` (nouveau fichier) ou `vault.write()` (fichier existant — régénéré à chaque run, pas de fusion par heading comme les rapports Markdown puisque le contenu est entièrement généré). N'avance jamais `auditChangesCursor`.
- Étend `src/settings/types.ts` : `changesHtmlPath: string` (défaut `""`), normalisé comme `reportPath` (`normalizeVaultPath`).
- Étend `src/settings/SettingsTab.ts::renderScope` (ou nouvelle sous-section "Change log HTML") : champ texte + `VaultPathSuggest` sur `folderCandidates()`.
- Étend `src/commands/auditCommands.ts` : `runChangesHtmlExport()`. Étend `src/commands/registry.ts` : commande `audit-changes-html` ("Export change log as HTML"), section Vault.
Valeurs fixes introduites : `changesHtmlPath: ""` (1 réglage visible, cohérent avec `reportPath`) ; suffixe de taille d'avatar `/50.png` et `memberCreator_fields` — détails de protocole Trello non configurables, même statut que `CARD_FIELDS`/`ACTIONS_PAGE_LIMIT`.
Périmètre — IN :
- Commande séparée "Export change log as HTML" (palette + panneau latéral, section Vault), indépendante du moment où "Audit changes" (Markdown) a été lancé.
- Avatar Trello de chaque auteur, téléchargé une fois par url unique et intégré en base64 dans le HTML (fonctionne hors-ligne, un seul fichier).
- Repli gracieux (initiale de l'auteur) si l'avatar est absent ou le téléchargement échoue — jamais d'échec de toute la commande pour un seul avatar manquant.
- Même regroupement jour→carte que le changelog Markdown (cohérence visuelle entre les deux vues), réutilisant `groupChangesByDayThenCard` exportée du sous-plan 1.
Périmètre — OUT :
- Lien direct vers la carte Trello dans le HTML — `AuditEntry` n'a pas l'url de la carte aujourd'hui (seulement `cardName`), l'ajouter est un champ de plus à threader partout pour un gain mineur ; à revisiter si demandé explicitement.
- Avancement de `auditChangesCursor` par cette commande — reste la responsabilité exclusive de "Audit changes" (Markdown), pour ne pas faire disparaître des entrées de l'audit canonique juste parce que la page HTML a été générée entre-temps.
- Pagination multi-pages / historique complet au-delà d'`ACTIONS_PAGE_LIMIT` — même limite déjà acceptée pour "Audit changes" (sous-plan `feature-audit-changes`).
- Cache persistant d'avatars entre deux exécutions de la commande — dédupliqué seulement en mémoire, pour la durée d'un run (YAGNI, un avatar change rarement et le coût est un fetch par auteur unique par génération, pas par entrée).

### Scénario utilisateur
- Nominal : l'utilisateur configure une fois le chemin de la page (`Projects/Trello Changes.html`), lance "Export change log as HTML" — une page s'ouvre (via le gestionnaire de fichiers du système ou un aperçu Obsidian) montrant, jour par jour puis carte par carte, qui a fait quoi, avec l'avatar de chaque auteur à côté de son nom.
- Échec : chemin non configuré → erreur explicite ("set it in the plugin settings"), même message que `reportPath` ; un auteur dont l'avatar ne charge pas (hors-ligne au moment du run, url invalide) → son entrée s'affiche quand même, avec un cercle-initiale à la place de la photo.

### Réutilisation
Composant existant le plus proche : `features/auditChanges.ts` (même forme exacte : fetch actions → `describeAction()` → construire un rendu → écrire dans le coffre). Non fusionné dans le même fichier : la sortie (HTML autonome avec avatars) et la règle de curseur (jamais avancé) diffèrent assez pour justifier une fonction séparée plutôt qu'un paramètre conditionnel dans `auditChanges()` — un `if (format === "html")` aurait mélangé deux responsabilités dans une seule fonction déjà chargée.

### Cas de test critiques
- `tests/changesHtml.test.ts` (pur, `core/changesHtml.ts`) : avatar présent → `<img src="data:image/png;base64,...">` ; avatar absent (`avatarDataUri: null`) → cercle-initiale, pas de balise `<img>` cassée ; même regroupement jour→carte que `buildChangesReport` (cohérence visuelle) ; liste vide → message explicite, pas de page vide silencieuse.
- `tests/exportChangesHtml.test.ts` (`features/exportChangesHtml.ts`, `FakeVault` + `clientFor` étendu + fetcher d'avatar factice) : deux entrées du même auteur → un seul appel au fetcher d'avatar (déduplication par url) ; le fetcher renvoie `null` pour une url → l'entrée correspondante a `avatarDataUri: null`, pas d'exception ; le fichier est créé s'il n'existe pas, réécrit s'il existe déjà ; `auditChangesCursor` inchangé après l'appel, y compris hors dry-run (contrairement à `runChangesAudit`).
- Chemin non configuré (`changesHtmlPath === ""`) → erreur explicite, même comportement que `requireReportNote`.

## Phases
1. TDD core : `TrelloAction.memberCreator.avatarUrl`/`AuditEntry.authorAvatarUrl` (passthrough dans `describeAction()`, cas de test ajouté à `tests/auditAction.test.ts`) ; export de `groupChangesByDayThenCard` ; `core/changesHtml.ts::buildChangesHtml()` (rouge → vert, `tests/changesHtml.test.ts`).
2. `trello/client.ts::getActions()` (`memberCreator_fields`) ; `obsidian/transport.ts::obsidianDownloadBinary()`.
3. TDD `features/exportChangesHtml.ts` (rouge → vert, `tests/exportChangesHtml.test.ts`, fetcher d'avatar factice) ; `settings/types.ts::changesHtmlPath` + normalisation.
4. `SettingsTab.ts` (champ + autocomplétion dossier) ; `commands/auditCommands.ts::runChangesHtmlExport()` ; `commands/registry.ts` (commande `audit-changes-html`).
5. `npm run check` + greps §8 + code-review (2 sous-agents) + déploiement `test-vault/` + clôture (§5).

## Écarts

- **code-review (§7)** en 2 sous-agents parallèles sur un diff plus large que les sous-plans précédents. Spec : conforme avec **1 écart réel** — le cas de test "curseur inchangé" prévu par le plan n'existait pas (aucune assertion sur `auditChangesCursor` dans la suite) ; corrigé en ajoutant `tests/auditCommands.test.ts` (nouveau fichier, premier test de la couche `commands/` — `CommandContext` factice minimal, réutilisable si d'autres commandes ont besoin d'être testées directement). Standards : aucun finding bloquant ; 3 corrigés — (1) `"(no card)"` dupliqué entre les deux renderers → extrait en `cardDisplayLabel()` (`core/auditReport.ts`, partagé) ; (2) extraction de l'heure dupliquée → `entryTime()` (même fichier, partagé) ; (3) `obsidianDownloadBinary` avalait l'échec sans log → `exportChangesHtml.ts::resolveAvatar` logge désormais un `warn` explicite à chaque avatar manqué (cohérent avec le patron déjà en place dans `syncFolder.ts`). 1 point confirmé intentionnel (pas un finding) : `changesHtmlPath` autocomplète sur les dossiers, pas les fichiers Markdown existants comme `reportPath` — commentaire ajouté dans `SettingsTab.ts` pour expliciter la raison (page générée, n'existe généralement pas encore).
- **Preuve visuelle (§10) toujours impossible** : `request_access` refusé, blocage transversal inchangé depuis le 2026-09-04. Code + `npm run check` (308 tests) + déploiement `test-vault/` faits.
- Version bumpée en **1.6.0 (MINEUR)** — nouvelle commande observable par l'utilisateur (critère 3 de la règle §6), pas une simple continuation. Le "Terminé quand" prévoyait explicitement le blocage §10 connu — condition remplie, renommage `✅`.
- **Feuille de route 2026-09-05 close** (3/3 sous-plans livrés) : changelog reformaté (v1.5.6), journal persistant (v1.5.7), export HTML avec avatars (v1.6.0). `push`/`commit`/`release` (avec assets `main.js`/`manifest.json`/`styles.css` attachés, nouvelle règle CONTEXT.md §6) restent à faire — pré-autorisés par l'utilisateur, prochaine étape de cette session.
